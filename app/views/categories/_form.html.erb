<%= form_with model: @category, local: true do |form| %>

  <div class="field">
    <%= form.label :title %><br />
    <%= form.text_field :title, autofocus: true %>
  </div>

  <p>
    <%= form.submit %>
  </p>

<% end %>
